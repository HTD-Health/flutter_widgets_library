name: spring_loader_example
description: Spring loader example.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  spring_loader:
    path: ../

flutter:
  uses-material-design: true
